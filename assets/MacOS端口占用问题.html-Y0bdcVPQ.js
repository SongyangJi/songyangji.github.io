import{_ as e}from"./plugin-vue_export-helper-DlAUqK2U.js";import{o as d,c as s,a}from"./app-CN-Tp3xY.js";const t={},i=a(`<h2 id="端口占用问题" tabindex="-1"><a class="header-anchor" href="#端口占用问题" aria-hidden="true">#</a> 端口占用问题</h2><p>在macOS上，你可以使用以下命令来查看占用某个特定端口的进程：</p><ol><li><strong>使用<code>lsof</code>命令：</strong></li></ol><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>lsof -i :&lt;端口号&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>例如，要查看占用8080端口的进程：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>lsof -i :8080
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这将显示与该端口相关的所有进程信息，包括进程ID (PID)。</p><ol><li><strong>使用<code>netstat</code>命令：</strong></li></ol><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>netstat -anv | grep &lt;端口号&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>例如，要查看8080端口：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>netstat -anv | grep 8080
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol><li><strong>使用<code>ps</code>和<code>grep</code>命令结合<code>lsof</code>：</strong></li></ol><p>如果你想进一步获取详细的进程信息，可以结合<code>ps</code>和<code>lsof</code>命令。例如，要查看8080端口的进程详细信息：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>ps -ef | grep $(lsof -t -i :8080)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol><li><strong>使用<code>sudo lsof</code>获取更详细的信息：</strong></li></ol><p>有时候需要使用<code>sudo</code>权限来获取更详细的信息：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>sudo lsof -i :&lt;端口号&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>例如：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>sudo lsof -i :8080
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这些命令将帮助你找到占用特定端口的进程，并提供相应的进程ID和其他相关信息。</p>`,20),l=[i];function n(o,c){return d(),s("div",null,l)}const v=e(t,[["render",n],["__file","MacOS端口占用问题.html.vue"]]);export{v as default};
